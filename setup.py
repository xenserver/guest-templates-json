#!/usr/bin/env python3

from setuptools import setup

setup(name='guest-templates',
      version='1.0',
      description='Creates the default guest templates',
      packages=['guesttemplates'],
      scripts=['create-guest-templates'],
     )
