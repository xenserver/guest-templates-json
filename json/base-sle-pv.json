{
    "name_description": "Template that allows VM installation from Xen-aware SLES-based distros. To use this template from the CLI, install your VM using vm-install, then set other-config-install-repository to the path to your network repository, e.g. http://<server>/<path> or nfs:server:/<path>",
    "derived_from": "base-pvlinux.json",
    "min_memory": "512M",
    "disks": [ { "size": "8G" } ],
    "PV_args": "console=ttyS0 xencons=ttyS",
    "other_config": {
        "install-distro": "sleslike"
    }
}

